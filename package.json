{
  "name": "gitflowui",
  "main": "index.cjs",
  "type": "module",
  "dependencies": {
    "electrobun": "^0.0.1",
    "gitflowui": "file:"
  },
  "scripts": {
    "start": "bun run build:dev && electrobun dev",
    "build:dev": "bun install && electrobun build"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "electron": "23.1.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
